import React, { useState } from 'react';

export function MediaQueries(screenWidth, cssStyle) {
	// screenWidth = window.innerWidth;
	const [style, setStyle] = useState(cssStyle);

	// useEffect(() => {

	// }, []);

	
	// const styleCSS = {
	// 	width: cssStyle,

	// }

	return (
		<React.Fragment>

			{/* {console.log('screenWidth: ', screenWidth)} */}
			
		</React.Fragment>
	)
}